//* math object in javascript

//! Math.PI
const pivalue = Math.PI
console.log(pivalue);

//! Math.abs() : static method that returns the absolute value of a number
//or in simple jow far the number is from 0 it remains only positive

const absVal = Math.abs();
console.log(absVal);

//!Math.round(x) : rounds a number to the nearest integer

const roundedVal = Math.round(4.5);
console.log(roundedVal);

const ceil =  Math.ceil(4.7)
console.log(ceil);

const floor =  Math.floor(4.7)
console.log(floor);

//! trunc ; returns integer value
const values = Math.trunc(3.333333);
console.log(values);

//math.pow sqrt log

//! Math.random 
const randomVal = Math.random() * 100;
console.log(Math.round(randomVal));
